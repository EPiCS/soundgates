/*
 * Synthesizer.c
 *
 *  Created on: Nov 26, 2013
 *      Author: gwue
 */

#ifndef SYNTHESIZER_C_
#define SYNTHESIZER_C_



void doStuff() {
	/*
	 * Create components
	 * 	-> Allocate memory, initialize parameters
	 *
	 * 	Connect components
	 * 	-> Connect components via SoundLinks (ie. set the reconos read and write buffer addresses to those on the links)
	 *
	 * 	Finalize
	 * 	-> Initialize ReconOS stuff
	 *
	 * 	while(1)
	 * 		foreach component
 	 *			run()
 	 *		foreach component
 	 *			join()
 	 *			updateParameters()
 	 *		foreach link
 	 *			switchbuffers()
 	 *
	 *
	*/
}






#endif /* SYNTHESIZER_C_ */
